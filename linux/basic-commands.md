# Comandos básicos de Linux

Listado de comandos básicos utilizados en Linux como base para el aprendizaje en ciberseguridad.

## Navegación por el sistema de archivos
- `pwd` → muestra el directorio actual
- `ls` → lista archivos y directorios
- `ls -la` → lista detallada, incluyendo archivos ocultos
- `cd` → cambiar de directorio
- `cd ..` → subir un nivel

## Gestión de archivos y directorios
- `mkdir` → crear directorios
- `rmdir` → eliminar directorios vacíos
- `rm` → eliminar archivos o directorios
- `cp` → copiar archivos o directorios
- `mv` → mover o renombrar archivos

## Visualización de contenido
- `cat` → mostrar contenido de un archivo
- `less` → visualizar archivos de forma paginada
- `head` → mostrar las primeras líneas de un archivo
- `tail` → mostrar las últimas líneas de un archivo

## Permisos básicos
- `chmod` → modificar permisos
- `chown` → cambiar propietario de archivos

## Información del sistema
- `whoami` → usuario actual
- `uname -a` → información del sistema
- `df -h` → espacio en disco
- `free -h` → uso de memoria

---

📌 Archivo creado como parte del aprendizaje inicial de Linux orientado a ciberseguridad.
